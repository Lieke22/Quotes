namespace :quotes do
  desc "Rake task to get quotes data"
  task :fetch => :environment do
    puts "Quote:fetch"
#    http://www.iheartquotes.com/api/v1/random

#    quotes = Quote.quote_search
#   quotes.each do |item|
#     item.each do |hash|
#       @quote = Quote.new({
        # Code to instantiate an event
#       })
#       @quote.save
#     end
#   end
#   puts "#{Time.now} - Success!"
  end
end
